# Django 핵심 패키지
Django>=4.2.0
djangorestframework>=3.14.0

# Django Channels (WebSocket 지원)
channels>=4.0.0
daphne>=4.0.0
channels-redis>=4.2.0  # Redis 기반 Channel Layer (멀티프로세스/컨테이너 필수)

# 데이터베이스
psycopg2-binary>=2.9.0

# CORS 지원
django-cors-headers>=4.0.0

# 데이터 처리
pandas>=2.0.0,<2.3.0  # NumPy 2.3.x와의 호환성 문제로 버전 제한
numpy>=1.24.0,<2.0.0  # Illegal instruction 방지를 위해 NumPy 1.x 사용

# 외부 API
finance-datareader>=0.9.50
requests>=2.31.0
websocket-client>=1.6.0
opendartreader>=0.1.6  # DART API 편리한 접근을 위한 라이브러리

# 환경변수 관리
python-dotenv>=1.0.0

# 프로덕션 서버
gunicorn>=21.0.0 
