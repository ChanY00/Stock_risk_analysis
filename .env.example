# Root env for docker-compose variable interpolation
# These values are read at compose parse time, not from service env_file.

DB_NAME=kospi
DB_USER=postgres
DB_PASSWORD=postgres

# Optional: used by backend service overrides
DB_HOST=postgres
DB_PORT=5432

